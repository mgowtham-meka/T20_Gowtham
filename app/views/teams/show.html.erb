<h1>Team Details</h1>

<p>
  <strong>Name:</strong>
  <%= @team.name %>
</p>

<h3>Players</h3>
<ul>
  <% @team.players.each do |player| %>
    <li><%= player.name %></li>
  <% end %>
</ul>

<%= link_to 'Edit', edit_team_path(@team) %> |
<%= link_to 'Back', teams_path %>
